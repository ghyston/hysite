@page "{tagName}"


@model TagPageModel

<h3>Posts tagged <b>@Model.Tag</b>:</h3>

<table>
@foreach (var item in @Model.Posts)
{
    <tr>
        <td>@item.Created.ToString("MMM dd, yyyy")</td>
        <td>&nbsp;</td>
        <td><a href=~/@item.FileName>@item.Title</a></td>
    </tr>
}
</table>
